
https://github.com/asdf-vm/asdf-erlang?tab=readme-ov-file

# Plug

https://hexdocs.pm/plug/Plug.Logger.html

A plug for logging basic request information in the format:
GET /index.html
Sent 200 in 572ms

plug Plug.Logger, log: :debug

# sudo apt-get install build-essential autoconf libncurses5-dev openssl libssl-dev fop xsltproc unixodbc-dev git
sudo apt-get update
sudo apt-get install build-essential autoconf libncurses5-dev openssl libssl-dev unixodbc-dev
sudo apt install libsctp-dev
sudo apt-get install systemtap-sdt-dev

# Erlang install
kerl update releases
kerl build 28.1 28.1
kerl install 28.1 ~/kerl/28.1

# Elixir
kiex list releases
kiex install 1.18.4
## https://github.com/lexical-lsp/lexical/pull/849
kiex install 1.18.1

# Compile Erlang from source
./configure --prefix=/home/z/appslnx/elixir/erlang
make
make install

mix new x --sup
mix archive.install hex phx_new
https://hexdocs.pm/phoenix/Mix.Tasks.Phx.New.html
mix phx.new pro-dir --module Pro --app pro --no-html --no-assets --no-ecto --no-mailer --no-gettext --no-dashboard --no-live

# https://hexdocs.pm/mix/Mix.Tasks.New.html
mix new exercise_1 --module Fetcher

» mix hex.docs fetch ecto
Docs fetched: /media/a/data/repo/.hex/docs/hexpm/ecto/3.5.3
» mix hex.docs offline ecto

# testing
# https://brooklinmyers.medium.com/phoenix-elixir-testing-beyond-mix-test-5b07de241001
mix test --slowest 100
# Elixir scripts
Mix.install([
  {:req, "~> 0.2"},
  {:floki, "~> 0.30.0"}
])

resp = Req.get!("https://github.com/elixir-lang")

resp.body
|> Floki.parse_document!()
|> Floki.find(~s{[data-hovercard-type="repository"]})
|> Enum.map(&Floki.text/1)
|> Enum.map(&String.trim/1)
|> Enum.each(&IO.puts/1)

$ elixir elixir-repos.exs

# System.trap_signal/3 --> Traps the given signal to execute the fun.

https://github.com/jdev-ops/phoenix_template
https://github.com/jdev-ops/ex_demo

https://github.com/zoedsoupe/peri
https://github.com/elixir-toniq/norm
https://github.com/beam-telemetry/telemetry_metrics



## JWT
/home/z/src/elixir/pocs/incubation/user-node-incubation/custom_deps/jwt_service